# H2 复核与修复记录

日期：2026-09-21。对象：Luna 完成阶段 3 后的工作区。复核期间保留已有未提交修改，未修改原 MFC 工程。

**结论：H2 通过。工程复核和修复检查通过，用户已在修复后试玩确认“可以了”。** 原交付的地图支撑、护栏一致性、测试场隔离、就近复位和镜头避障问题已直接修复。真实窗口已打开，并观察到车辆沿正式环路行驶；用户报告轮胎像透明、车身像悬空；模型朝向、车身高度和接地阴影已修正，用户复看确认后关闭本轮问题。当前美术是低多边形基础版，后续仍需专门完善。

## 发现与处理

| 原问题 | 修复结果 |
|---|---|
| 12.8 米可见路肩只有 8.6 米道路碰撞，抬高路段可能踩空 | 路面和两侧路肩均使用与显示相同的三角网格 |
| 物理护栏每段都有，显示却只画隔段，且高度、宽度不同 | 两侧连续护栏共用实际网格，无隐藏段 |
| 道路拼接盒与显示坡度存在差别；路面和路肩重叠闪烁 | 移除拼接盒，使用连续网格；路肩只绘制两侧带状区域 |
| 海下有无限测试地面，旧墙/坡道留在正式世界；路面识别混入测试区 | 正式地图与测试世界明确分开；海面无支撑平面 |
| 高架道路旁树木浮在平地上，悬崖只是零散盒子 | 陆地/悬崖连续连接道路，装饰按真实地面高度放置 |
| 椭圆路线缺乏直道和不同弯型 | 723 米路线增加直道、上坡弯和 S 弯，保留起点位置 |
| 最近点只取离散顶点，R 无论哪里都回起点 | 投影到路段，R 就近回路中并按坡度扶正 |
| 相机不检测遮挡，掉海复位仍可能插值穿过场景 | 相机球体扫掠；自动复位当帧同步前后快照 |
| 拆出的轮胎漏掉原模型朝向校正，轮毂朝内；车身轮拱高约 33 cm | 轮胎保留 180° 校正，车身轮拱与静止轮心对齐 |
| 默认阴影偏移使接触阴影远离轮胎，加重悬空观感 | 按当前场景深度减小阴影偏移，并检查两侧近景 |
| 45/60 秒测试只验证短时行驶，未证明整圈和车轮接地 | 有序路线进度检查两个整圈；采样点实测四轮接触；逐段检查接缝 |

## 保留的车辆手感

`vehicle_config.py` 中的车辆参数未改。只把原有转向上限计算提取为可复用函数，公式保持一致。渐进踏板、扭矩曲线、五挡自动变速、发动机制动、连续转向/回正与追尾缓冲继续保留。

| 指标 | 本轮复测 |
|---|---:|
| 0–100 km/h | 9.383 秒 |
| 100 km/h 刹停 | 42.345 米 / 2.950 秒 |
| 全油门 22 秒 | 154.245 km/h |
| 停车 15 秒后车轮接地 | 4/4，随后正常起步 |

近处路缘、虚线、反光片和路面颗粒改善速度参照；未夸大世界位移或改速度表来制造速度感。

## 验收证据

- **70 项 pytest 通过**；Ruff 通过。包括无显示 10000 步、30/60/144 FPS 仿真一致性与已有驾驶响应回归。
- 每一个路段起点/接缝及中点，在中心和两侧路肩进行向下查询，共 1536 处，支撑高度误差小于 1.5 cm；两侧护栏共 512 处横向射线命中。
- 8 个道路位置实测四轮接地；最高路段两侧路肩实车停车稳定。
- 60 / 120 km/h 斜向护栏碰撞未穿透；R、掉海恢复、复位插值与镜头避障检查通过。
- 两组各两个整圈：

| 巡航上限 | 实际最高速度 | 两圈时间 | 最大中心线偏差 | 最大侧倾 | 重置 / 四轮全离地 |
|---|---:|---:|---:|---:|---:|
| 60 km/h | 62.2 km/h | 103.88 s | 0.578 m | 0.28° | 0 / 0 |
| 90 km/h | 74.2 km/h | 101.03 s | 0.579 m | 0.30° | 0 / 0 |

跟随脚本会在弯前减速，不能将第二行表述为“90 km/h 全程跑圈”。

- `tools/h0_ui_check.py`、`tools/h1_ui_check.py` 通过：菜单、倒计时持键、raw/方向键、暂停、失焦、复位、重开和轮胎姿态映射均有回归。
- `tools/h2_wheel_check.py` 检查 4 个旋转角、左右两侧近景；4 轮接触真实路面，静止轮拱与轮心高度/前后位置误差小于 3 cm，旋转后视觉轮心误差小于 1 mm，胎底距路面在 -2 至 +3 cm。8 张近景与数值报告保存在 `logs/h2-review/wheels-after`。
- 实际应用离屏渲染完整一圈，1280×720、阴影、MSAA4、RTX 4060 Laptop：轮胎几何修复后平均 **204.4 FPS**，P95 **6.50 ms**，P99 **7.24 ms**。5 张途中截图保存在 `logs/h2-review/render`。启动和同步保存截图期间累计丢弃 0.150 秒补进时间；该检查不是独占环境下的屏幕刷新率测试。
- 静态场景合批，地图碰撞由原来的数百个小刚体改为 7 个静态网格体。
- 重新构建 **0.3.0** 文件夹版，从系统临时目录启动 exe 的离屏检查通过，模型/纹理/中文字体正常，重开 20 次的节点、任务和事件数量稳定。

主要记录：`logs/h2-review/route-check.json`、`logs/h2-review/render/report.json`、`logs/h2-review/wheels-after/report.json`、`logs/h2-review/packaged/h0-render-smoke.json`。完整画面应直接查看该目录中的 PNG，而非仅依据构建成功判断显示正确。

## 代码与后续交接

路线与网格集中在 `coastal_map.py`；物理只在 `simulation.py`；窗口读取快照及只读场景查询。复核没有引入通用框架、大量兜底异常或复杂配置层。地图验收脚本放在 `tools`，不混入玩家控制器。

Luna 下一阶段可继续游戏流程与计时/检查点，但需先读取原阶段计划、`current-progress.md` 和本记录。保持当前驾驶参数；比赛进度必须在仿真侧计算，不能靠画面帧数，不能直接把“最近点跳过终点”当有效完圈。复位、逆行、跳过检查点和重开需有明确规则。AI 训练和驾驶模式仍留到对应阶段。

本轮未完成最终美术、音效、车辆损伤或交通碰撞。打包仍有已有的 `api-ms-win-core-path-l1-1-0.dll` 依赖查找及可选模块提示；本机独立版通过，但尚未在干净 Windows 机器测试。
