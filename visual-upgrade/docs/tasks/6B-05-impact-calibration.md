# 6B-05 真实驾驶校准与音频验收

- 状态：done（v1 碰撞音效主观验收）。用户在 2026-09-23 实际试听当前 0.8.3 后表示“现在的碰撞音效还不错，我的判断是通过了”。八种预设场景未逐项提供日志或结论；保留为后续回归清单，不伪称逐项通过。设计基线 `34cec49231391c769ef38db1ee2e22363863c20b`。
- 实施：Luna 负责记录分析、参数与重复机械检查；用户负责实际驾驶听感结论。仅在架构违背证据或两轮实质修复失败时升级。
- 读 [设计](../vehicle-impact-audio.md) 第 7–11 节、前序包交接；不重审整个项目。

## 范围

- 允许：impact-bank.json 的曲线/配方/声音预算内参数、必要素材修剪与来源记录；物理检测配置仅依据新增原始数据校准；诊断工具、音频专项回归、本文、current-progress。
- 禁止：驾驶/交通/道路/固定步/比赛/音量设置改变；不重引入渲染速度差、长全局 cooldown，不通过放宽测试让噪声墙通过。
- 首先保留原始数据；改同一参数只改变一个明确因素，记录前后配置 hash。无需重跑已经有效且未受影响的整套测试。

## 八项人工记录

使用 6B-04 的 `tools/impact_audio_check.py --drive --output <新目录>`，正常设备/正常音量，进入驾驶并聚焦游戏窗口。每项保留接近速度、物体、event_id、severity、layers、scrape 与主观结论。

| 场景 | 通过标准 | 用户结论 |
|---|---|---|
| 低速护栏轻碰 | 及时可辨，短而不刺耳 | 未逐项记录 |
| 中速正面护栏 | 比轻碰明显，有车体反馈 | 未逐项记录 |
| 高速重撞 | 重量/结构/少量碎片，发动机短暂让位 | 未逐项记录 |
| 高速侧擦 | 初撞与连续摩擦自然衔接，不假装重撞串 | 未逐项记录 |
| NPC 车辆 | 相对运动驱动强度，有车身质感 | 未逐项记录 |
| 旋转后第二击 | 200–500 ms 后的新接触能再次清楚响起 | 未逐项记录 |
| 连续擦碰 ≥5 s | 循环连续、退出利落、无机关枪声 | 未逐项记录 |
| 翻滚/多次落地 | 当前物理能产生的实体接触有节奏且不过载 | 未逐项记录 |

另做同类 ≥10 次重复检查，轻→重动态对比，静音/暂停/菜单/重开验证。明确区分轮胎悬架落地与底盘 manifold 撞地；不能为第8项修改物理或临时做假事件。自动夹具与用户能否在游戏中复现分别记账。

## 校准与验证

1. 从真实驾驶的静置/擦碰负样本核查物理噪声门槛，不让支持力/点替换进入 Impact。
2. 以轻/中/重代表区间冻结单调 log 曲线；核查 raw/excess 差异是否误吞第二击；高 vt、低 vn 应主要是 scrape。
3. 以相同日志/随机种子重放，对比 layer 电平、池内一致性、重触发与声音预算；离线输出峰值分析，真设备确认动态和音色。
4. 记录提交时间与回放提交延迟；若要端到端数字，用带游戏画面的系统音频录制测量，不能拿软件内部计时冒充扬声器延迟。

- T0/T1：用项目解释器执行 `tools/validate.py T0 --area audio` 与 `T1 --area audio`；若调整物理检测参数，加 core/gameplay。只对新改动补相关检查。
- T2：6B-04 有效结果可复用，除非接口/行为修改使之失效；T3 属整体6B/交付阶段，不自动启动。
- v1 验收结论以用户对当前实驾声音的明确整体判断为准；上表仍是更细的回归矩阵，未逐项试听的场景不能单独勾选通过。若将来声音退化或针对某种事故再调参，用诊断记录复现。碰撞音效通过不等于其他 6B 画面/性能缺口完成。

## 交接

- 0.8.3 新系统已实现，用户实驾听感通过；旧 6B-01 碰撞声音不通过的历史结论保留。未收到本次驾驶的原始 JSONL，因此初始曲线保留 provisional，未据此宣称定量校准完成。
- 6B-02–04 实现提交为 `ec2a5f2`；本次只登记用户结论与未逐项验证边界，不改声学参数、素材或玩法规则。
